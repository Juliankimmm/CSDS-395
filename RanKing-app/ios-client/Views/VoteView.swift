import SwiftUI

struct VoteView: View {
    var body: some View {
        VStack {
            Text("Vote on Outfit")
            // TODO: show two images side by side
            HStack {
                Button("Outfit A") {}
                Button("Outfit B") {}
            }
        }
    }
}
