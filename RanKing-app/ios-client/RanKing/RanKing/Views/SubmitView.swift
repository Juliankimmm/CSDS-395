import SwiftUI

struct SubmitView: View {
    var body: some View {
        VStack {
            Text("Submit Fashion Image")
            Button("Upload Image") {
                // TODO: implement image picker + API upload
            }
        }
    }
}
